version: '3'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    ports:
      - "3001:3000"  # ホストの3001番ポートをコンテナの3000番ポートにマッピング
